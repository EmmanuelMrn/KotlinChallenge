<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mindteamschallenge">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_kotlin"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_kotlin_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MindTeamsChallenge">
        <activity android:name=".user.UserScreenActivity"></activity>
        <activity android:name=".superadmin.SuperAdminScreenActivity" />
        <activity android:name=".admin.AdminScreenActivity" />
        <activity android:name=".home.HomeActivity" />
        <activity android:name=".login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>